#include <stdio.h>
#pragma warning(disable:4996)

int main() {
	int a, c[1000], score = 0;
	char b[1000];
	

	scanf("%d", &a); 
	scanf("%s", b); #공백을 없애기 위해 문자열로 입력
	for (int i = 0; i < a; i++) {
		c[i] = b[i] - '0'; #아스키코드를 이용
		score += c[i];
	}
	
	printf("%d", score);

}
