#include <stdio.h>
#pragma warning(disable:4996)

int main() {
	int num;
	int max = -1000000, min = 1000000;
	int a;
	scanf("%d", &num);
	for (int i = 0; i < num; i++) {
		scanf("%d", &a);
		if (a >= max) max = a;
		
		if (a <= min) min = a;
	}
	printf("%d %d", min, max);
}
